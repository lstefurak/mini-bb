// M0 stub — the real CLI (clap derive, FR-1/FR-6) lands in M1/M2.
fn main() {
    println!(
        "mini-bb (spec v{}) — see SPEC.md; `index` and `search` arrive in M1/M2",
        mini_bb::SPEC_VERSION
    );
}
